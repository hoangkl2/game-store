import { expect, test } from "@playwright/test";
test("mock room explains and validates start requirements", async ({ page }) => { await page.goto("/rooms/DEMO42"); await expect(page.getByRole("heading", { name: "Player slots" })).toBeVisible(); await expect(page.getByRole("button", { name: "Start game" })).toBeDisabled(); await page.getByRole("button", { name: "Add bot" }).click(); await page.getByRole("button", { name: "Ready" }).click(); await expect(page.getByRole("button", { name: "Start game" })).toBeEnabled(); await page.getByRole("button", { name: "Start game" }).click(); await expect(page.getByRole("status")).toContainText("Mock handoff complete"); });
