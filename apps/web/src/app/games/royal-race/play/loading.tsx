import { PageSkeleton } from "@/components/feedback/states";
export default function Loading() { return <main className="mx-auto max-w-[var(--content-max)] p-[var(--page-padding)]"><PageSkeleton /></main>; }
