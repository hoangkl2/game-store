import Link from "next/link";
import { AppShell } from "@/components/layout/app-shell";
import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";
import { catalogGames } from "@/features/game-catalog/catalog-data";
export default function GamesPage() { return <AppShell><header className="mb-8 max-w-[var(--readable-max)]"><p className="font-semibold text-primary">Game library</p><h1 className="mt-2 font-display text-4xl font-semibold">Find your next table.</h1><p className="mt-2 text-muted-foreground">Filter and search are planned; this local catalog shows availability truthfully.</p></header><div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{catalogGames.map((game) => <Card key={game.slug} className="flex flex-col gap-4"><div className="h-28 rounded-gameplay" style={{ background: game.accent }} /><div className="flex items-center justify-between gap-2"><h2 className="text-xl font-bold">{game.name}</h2><Badge variant={game.status === "playable" ? "ready" : "offline"}>{game.status === "playable" ? "Playable" : "Planned"}</Badge></div><p className="text-muted-foreground">{game.description}</p><p className="text-sm text-muted-foreground">{game.players} · {game.duration}</p><div className="flex flex-wrap gap-2">{game.modes.map((mode) => <Badge key={mode} variant="neutral">{mode}</Badge>)}</div><Link className="mt-auto font-semibold text-primary hover:underline" href={`/games/${game.slug}`}>View details</Link></Card>)}</div></AppShell>; }
