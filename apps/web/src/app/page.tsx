import Link from "next/link";
import { AppShell } from "@/components/layout/app-shell";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { catalogGames } from "@/features/game-catalog/catalog-data";

export default function HomePage() { const uno = catalogGames[0]!; return <AppShell><section className="grid gap-6 lg:grid-cols-[1.3fr_0.7fr]"><div className="space-y-5 py-6"><p className="font-semibold text-primary">Guest play is ready</p><h1 className="font-display text-4xl font-semibold tracking-tight md:text-5xl">Choose a game. Start in minutes.</h1><p className="max-w-[var(--readable-max)] text-lg text-muted-foreground">Play offline with bots now, or explore games planned for local and online play.</p><div className="flex flex-wrap gap-3"><Link className="inline-flex min-h-11 items-center rounded-button bg-primary px-4 font-semibold text-primary-foreground" href="/games">Browse games</Link><Link className="inline-flex min-h-11 items-center rounded-button border border-border-strong px-4 font-semibold" href="/play/uno">Continue with UNO</Link></div></div><Card className="self-start"><CardHeader><CardTitle>Continue playing</CardTitle></CardHeader><CardContent className="space-y-3"><p className="font-semibold text-foreground">{uno.name} offline match</p><p>Start a new local session or resume the saved demo on this device.</p><Link className="inline-flex min-h-11 items-center rounded-button bg-primary px-4 font-semibold text-primary-foreground" href="/play/uno">Play UNO</Link></CardContent></Card></section><section className="mt-10"><div className="mb-4 flex items-end justify-between gap-4"><div><h2 className="text-2xl font-bold">Featured games</h2><p className="text-muted-foreground">Every game shows what is playable today.</p></div><Link className="font-semibold text-primary hover:underline" href="/games">View library</Link></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{catalogGames.slice(0, 4).map((game) => <Card key={game.slug} className="space-y-3"><div className="h-20 rounded-gameplay" style={{ background: game.accent }} /><h3 className="font-bold">{game.name}</h3><p className="text-sm text-muted-foreground">{game.status === "playable" ? "Playable offline" : "Coming soon"}</p><Link className="font-semibold text-primary hover:underline" href={`/games/${game.slug}`}>View details</Link></Card>)}</div></section></AppShell>; }
