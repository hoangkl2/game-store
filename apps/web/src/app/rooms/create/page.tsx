import Link from "next/link";
import { AppShell } from "@/components/layout/app-shell";
import { Card } from "@/components/ui/card";
export default function CreateRoomPage() { return <AppShell><header className="max-w-[var(--readable-max)]"><p className="font-semibold text-primary">Private room</p><h1 className="mt-2 font-display text-4xl font-semibold">Create a room</h1><p className="mt-2 text-muted-foreground">This development slice opens a local mock lobby. It does not create a realtime room or send invitations.</p></header><Card className="mt-8 max-w-[var(--readable-max)] space-y-4"><h2 className="text-xl font-bold">UNO room prototype</h2><p className="text-muted-foreground">Two players, private visibility, one optional bot. Server validation is represented only by requirements text.</p><Link className="inline-flex min-h-11 items-center rounded-button bg-primary px-4 font-semibold text-primary-foreground" href="/rooms/DEMO42">Open mock room</Link></Card></AppShell>; }
