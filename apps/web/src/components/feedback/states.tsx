import { Button } from "@/components/ui/button";
export function EmptyState({ title, description, action }: { title: string; description: string; action?: React.ReactNode }) { return <section className="rounded-card border border-dashed bg-surface p-8 text-center"><h2 className="text-xl font-bold">{title}</h2><p className="mx-auto mt-2 max-w-prose text-muted-foreground">{description}</p><div className="mt-5">{action}</div></section>; }
export function ErrorState({ title = "Something went wrong", description = "Try again. If the problem continues, use the diagnostic ID when contacting support." }: { title?: string; description?: string }) { return <section role="alert" className="rounded-card border border-danger bg-danger-subtle p-6 text-danger"><h2 className="text-xl font-bold">{title}</h2><p className="mt-2">{description}</p><Button className="mt-4" variant="outline" onClick={() => window.location.reload()}>Try again</Button></section>; }
export function PageSkeleton() { return <div aria-label="Loading page" className="space-y-5" aria-busy="true"><div className="h-10 w-64 animate-pulse rounded-control bg-muted" /><div className="grid gap-4 md:grid-cols-3">{[1, 2, 3].map((key) => <div key={key} className="h-56 animate-pulse rounded-card bg-muted" />)}</div></div>; }
